Following the rules of https://www.conventionalcommits.org/en/v1.0.0, create a commit message with the diff that you receive. 
Output the text as plain text with markdown. 
The commit summary (first line) MUST be a valid Conventional Commit.
The body MUST use bullet lists to describe the changes.
Focus on the "Why" behind the changes, especially if a context hint is provided.
Output as plain markdown without code fences around the entire output.

